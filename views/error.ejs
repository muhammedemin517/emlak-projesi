<!DOCTYPE html>
<html lang="tr">
<head>
    <%- include('partials/head') %>
</head>
<body>
    <%- include('partials/nav') %>
    <main class="container narrow">
        <div class="card error-wrap">
            <div class="code"><%= statusCode || 500 %></div>
            <h2 style="margin: 10px 0;"><%= title || 'Hata' %></h2>
            <p class="text-muted"><%= message %></p>
            <div class="mt-2">
                <a href="/dashboard" class="btn btn-primary">🏠 Panele Dön</a>
                <a href="/listings" class="btn btn-outline">📋 İlanlara Göz At</a>
            </div>
        </div>
    </main>
</body>
</html>
